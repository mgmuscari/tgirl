# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python (default template examples — replace with your stack)
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Node (if adapting for JS/TS)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Benchmark outputs
result/
benchmarks/*.log

# Claude Code session artifacts
.claude/scheduled_tasks.lock

# Worktrees (parallel sessions)
# Git worktrees are created outside this directory, but ignore any local artifacts
*.worktree

# Push Hands / Dialectic tier metadata — feature-branch-local, never tracked
.push-hands-tier
.dialectic-tier
